@model Examen
@{ViewData["Title"] = "Supprimer examen";}
<div class="row justify-content-center"><div class="col-lg-5"><div class="card-panel text-center">
    <div class="delete-icon mb-3"><i class="bi bi-file-earmark-x-fill text-danger"></i></div>
    <h5>Supprimer cet examen ?</h5>
    <div class="alert alert-warning text-start py-2 small">
        <strong>Étudiant :</strong> @Model.Etudiant?.NomComplet<br/>
        <strong>Module :</strong> @Model.Module?.Libelle<br/>
        <strong>Note :</strong> @Model.Note_Ex.ToString("0.00")/20
    </div>
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id"/>
        @Html.AntiForgeryToken()
        <div class="d-flex gap-2 justify-content-center">
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash me-1"></i>Supprimer</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Annuler</a>
        </div>
    </form>
</div></div></div>
